# 언어 간 실행 검사

각 사례는 같은 `goondan.yaml`과 템플릿, 입력·가짜 모델 응답을 사용합니다. `expected.json`은 모델이 받은 시스템 블록과 메시지, 최종 출력, 저장된 메시지 출처를 비교합니다. 식별자와 시각은 언어별로 달라질 수 있으므로 비교 대상에서 제외합니다.

`async-approval` 사례는 승인 대기 결과와 별도 완료 입력의 공통 직렬화 및 단일 전달 계약을 확인합니다.
