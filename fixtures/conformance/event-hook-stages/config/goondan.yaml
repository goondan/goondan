agents:
  main:
    model: m
    hooks:
      input:
        - name: tag
          fn: tagIt
      conversation:
        - fn: skipMe
          when: {fn: never}
      output:
        - fn: boom
          optional: true
routes: [main]
