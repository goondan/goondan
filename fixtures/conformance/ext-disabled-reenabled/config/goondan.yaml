agents:
  base:
    model: m
    extensions:
      memo:
        enabled: false
    hooks:
      onModelInput:
        - extension: memo
  child:
    inherit: base
    extensions:
      memo:
        enabled: true
