agents:
  main:
    model: m
    tools: [lookup]
    extensions:
      memo: {}
    hooks:
      onToolCall:
        - name: shortcut
          fn: giveResult
      onToolResult:
        - name: done
          extension: memo
