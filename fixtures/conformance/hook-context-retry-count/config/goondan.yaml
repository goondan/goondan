agents:
  main:
    model: m
    extensions:
      memo: {}
    hooks:
      modelInput:
        - name: ctx
          extension: memo
      modelResult:
        - fn: retryOnce
          using: input
