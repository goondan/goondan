agents:
  main:
    model: m
    tools:
      - tool: publish
        approval: required
    hooks:
      onToolCall:
        - name: gate
          fn: askApproval
        - name: after
          fn: mark
