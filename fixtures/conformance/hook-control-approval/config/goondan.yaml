agents:
  main:
    model: m
    tools:
      - tool: publish
        approval: required
    hooks:
      toolCall:
        - name: gate
          fn: askApproval
        - name: after
          fn: mark
