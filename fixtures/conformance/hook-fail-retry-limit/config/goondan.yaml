agents:
  strict:
    model: m
    hooks:
      onModelResult:
        - name: again
          fn: askRetry
  lenient:
    model: m
    hooks:
      onModelResult:
        - name: again
          fn: askRetry
          optional: true
