agents:
  base:
    model: m
    hooks:
      conversation:
        - agent: [x, y]
  x:
    model: mx
  y:
    model: my
  child:
    inherit: base
    remove:
      hooks:
        conversation: ["x+y"]
