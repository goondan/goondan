agents:
  main:
    model: m
    hooks:
      onModelInput:
        - name: hint
          fn: addHint
          role: user
      onOutput:
        - name: polish
          template: templates/polish.md
