agents:
  main:
    model: m
    systemMessage:
      text: 기본 지시
    extensions:
      seed: {}
    hooks:
      onStep:
        - extension: seed
      onModelInput:
        - name: note
          fn: opsText
          role: system
