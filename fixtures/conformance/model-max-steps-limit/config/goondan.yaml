agents:
  main:
    model: m
    tools:
      - echo
    hooks:
      onModelInput:
        - name: mark
          fn: markInput
          role: user
      onError:
        - name: capture
          fn: captureError
