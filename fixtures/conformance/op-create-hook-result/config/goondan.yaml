agents:
  main:
    model: m
    tools:
      - tool: danger
        approval: required
    hooks:
      toolCall:
        - name: shortcut
          fn: canned
      toolResult:
        - name: record
          fn: record
