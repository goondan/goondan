agents:
  main:
    model: mm
    tools:
      - agent: worker
    extensions:
      policy: {}
    hooks:
      onError:
        - extension: policy
  worker:
    model: wm
    extensions:
      policy: {}
    hooks:
      onError:
        - extension: policy
