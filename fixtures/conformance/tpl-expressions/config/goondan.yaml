agents:
  main:
    model: m
    params:
      mode: brief
      n: 1
      list: [a, b]
      tags: {items: [x]}
    hooks:
      conversation:
        - name: t
          template: templates/system.md
