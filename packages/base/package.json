{
  "name": "@goondan/base",
  "version": "0.1.1",
  "type": "module",
  "files": [
    "dist",
    "bundle.yaml"
  ],
  "scripts": {
    "build": "node ./scripts/clean-dist.mjs && tsc -p tsconfig.build.json && node ./scripts/copy-yaml.mjs",
    "prepublishOnly": "pnpm build",
    "test": "vitest run"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@goondan/core": "workspace:^"
  }
}
