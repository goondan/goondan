#!/usr/bin/env node
/**
 * Goondan CLI Entry Point
 *
 * This is the main entry point for the `gdn` command.
 * It imports and executes the CLI program.
 */

import { run } from "./index.js";

run();
