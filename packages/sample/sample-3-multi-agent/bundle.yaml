apiVersion: agents.example.io/v1alpha1
kind: Bundle
metadata:
  name: sample-3-multi-agent
spec:
  # 의존성: base 번들 (telegram connector, compaction extension 등)
  dependencies:
    - github.com/goondan/goondan/packages/base

  # 포함할 리소스 YAML
  include:
    - dist/tools/delegate/tool.yaml
    - dist/tools/code/tool.yaml
    - dist/tools/git/tool.yaml
